#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin>>t;
    
    while(t--){
        int n;
        cin>>n;
        
        vector<int> arr(n);
        
        for(int i=0; i<n; i++) cin>>arr[i];
        
        int curr= arr[n-1]- arr[0];
        
        for(int i=0; i<n-1; i++){
            curr= max(curr, arr[i]-arr[i+1]);
        }
        
        for(int i= 1; i<n; i++) curr= max(curr, arr[i]-arr[0]);
        
        for(int i=0; i<n-1; i++) curr= max(curr, arr[n-1]-arr[i]);
        
        cout<<curr<<'\n';
    }
    
    return 0;
}